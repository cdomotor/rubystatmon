<!-- File: app/views/layouts/_sidebar.html.erb -->
<!-- Path: /app/views/layouts/_sidebar.html.erb -->
<nav class="bg-gray-800 dark:bg-gray-900 text-white w-64 min-h-screen" aria-label="Primary">
  <ul class="space-y-2 p-4">
    <li class="mb-2">
      <%= render 'shared/theme_toggle' %>
    </li>

    <li>
      <%= link_to dashboard_path,
            class: "block py-2 px-3 rounded-md hover:bg-gray-700 dark:hover:bg-gray-800 #{'bg-gray-900 dark:bg-black/40' if current_page?(dashboard_path)}",
            aria: { current: (current_page?(dashboard_path) ? 'page' : nil) } do %>
        <span class="inline-flex items-center gap-2">
          <span aria-hidden="true">📊</span>
          <span>Dashboard</span>
        </span>
      <% end %>
    </li>

    <li>
      <%= link_to stations_path,
                  class: "block py-2 px-3 rounded-md hover:bg-gray-700 #{'bg-gray-900' if current_page?(stations_path)}" do %>
        <span class="inline-flex items-center gap-2">
          <span>📡</span>
          <span>Stations</span>
        </span>
      <% end %>
    </li>

    <li>
      <% if Rails.application.routes.url_helpers.respond_to?(:features_path) %>
        <%= link_to features_path,
                    class: "block py-2 px-3 rounded-md hover:bg-gray-700 #{'bg-gray-900' if current_page?(features_path)}" do %>
          <span class="inline-flex items-center gap-2">
            <span>✨</span>
            <span>Features</span>
          </span>
        <% end %>
      <% else %>
        <!-- Optional: show a disabled item instead of erroring -->
        <span class="block py-2 px-3 rounded-md text-gray-500 cursor-not-allowed">Features</span>
      <% end %>
    </li>

    <li>
      <%= link_to daemon_logs_path,
          class: "flex items-center gap-2 rounded-md px-3 py-2 text-sm font-medium #{current_page?(daemon_logs_path) ? 'bg-gray-900 text-white' : 'text-gray-300 hover:bg-gray-700 hover:text-white'}" do %>
        <span>📜</span>
        <span>Daemon Logs</span>
      <% end %>
    </li>

    <li>
      <%= link_to import_stations_path,
            class: "flex items-center gap-2 rounded-md px-3 py-2 text-sm font-medium #{current_page?(import_stations_path) ? 'bg-gray-900 text-white' : 'text-gray-300 hover:bg-gray-700 hover:text-white'}" do %>
        <span>📥</span>
        <span>CSV Import</span>
      <% end %>
    </li>


    

  </ul>
</nav>
