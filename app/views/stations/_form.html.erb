<!-- File: app/views/stations/_form.html.erb -->
<!-- Full path: /app/views/stations/_form.html.erb -->

<%= form_with(model: station) do |form| %>
  <% if station.errors.any? %>
    <div class="mb-4 text-red-600">
      <h2><%= pluralize(station.errors.count, "error") %> prohibited this station from being saved:</h2>
      <ul>
        <% station.errors.full_messages.each do |msg| %>
          <li>• <%= msg %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <!-- Your other fields ... -->

  <div class="mb-4">
    <%= form.label :configured_parameters, "Configured Parameters (JSON Array)", class: "block font-medium mb-1" %>
    <%= form.text_area :configured_parameters,
        value: station.configured_parameters.to_json,
        rows: 3,
        class: "w-full rounded border-gray-300" %>
    <p class="text-xs text-gray-500 mt-1">Example: ["Battery","flow_rate","turbidity","SignalStrength"]</p>
  </div>

  <div class="flex gap-2">
    <%= form.submit class: "px-4 py-2 rounded bg-blue-600 text-white hover:bg-blue-700" %>
    <%= link_to "Cancel", stations_path, class: "px-4 py-2 rounded bg-gray-100 hover:bg-gray-200" %>
  </div>
<% end %>
